import 'package:flutter/material.dart';

class CategoriesConfiguration extends StatelessWidget {
  const CategoriesConfiguration({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("cateogry configurations"),
    );
  }
}
